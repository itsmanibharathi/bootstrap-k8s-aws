---
- name: Ensure base packages are installed
  apt:
    name:
      - curl
      - wget
      - socat
      - conntrack
      - iptables
      - ebtables
      - ethtool
      - apt-transport-https
      - gnupg
      - rsync
    state: present
    update_cache: yes